interface:
  display_name: "Figma Workspace"
  short_description: "Figma automation through stateless independent npm leaf commands"
  default_prompt: "Use $figma-workspace and its public figma:* npm leaf commands for Figma work. Discover commands with figma:help, pass an explicit Figma URL or file/node target whenever the selected command or live schema requires one, use concise English docs query seeds for relevance with hard surface filters, read exact docs IDs, search Plugin API symbols with a readable selector and use qualified selectors to resolve ambiguity, run selected command help, create .figma.ts scripts in the shell, and verify mutations without blindly retrying outcome_unknown."
